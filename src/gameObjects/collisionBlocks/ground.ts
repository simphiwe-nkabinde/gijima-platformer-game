import { CollisionBlock } from "../../classes/CollisionBlock";

export default new CollisionBlock({
    position: { x: 0, y: window.innerHeight - 10 },
    width: window.innerWidth,
    height: 5,
    imageSrc: ''
})